# Liiiraa Booster root environment template.
# This file is safe to commit. Copy app-specific templates to ignored local
# .env files and keep real secrets out of git.

NODE_ENV=development
LIIRAA_ENV=local
PUBLIC_APP_NAME=Liiiraa Booster
PUBLIC_RELEASE_CHANNEL=dev
PUBLIC_API_BASE_URL=http://localhost:4010
LOG_LEVEL=info
