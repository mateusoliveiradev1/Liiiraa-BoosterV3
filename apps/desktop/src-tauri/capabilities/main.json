{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Default-deny capability for the main Liiiraa Booster window. It grants only the read-only typed IPC security status command and no filesystem, shell, process, dialog, HTTP, clipboard, notification, updater, OS, plugin, or privileged custom command permissions.",
  "windows": ["main"],
  "platforms": ["windows"],
  "local": true,
  "permissions": ["allow-get-ipc-security-status"]
}
