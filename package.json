{
  "name": "liiiraa-booster",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "check": "pnpm check:js && pnpm check:rust",
    "check:js": "turbo run check",
    "check:rust": "cargo check --workspace --all-targets",
    "commitlint": "commitlint",
    "desktop:build": "pnpm --filter @liiiraa/desktop tauri:build",
    "desktop:dev": "pnpm --filter @liiiraa/desktop tauri:dev",
    "dev": "turbo run dev",
    "format": "turbo run format",
    "lint": "turbo run lint",
    "lint-staged": "lint-staged",
    "prepare": "husky",
    "test": "turbo run test",
    "turbo": "turbo run build",
    "typecheck": "turbo run typecheck"
  },
  "lint-staged": {
    "*": "git diff --check --cached --"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.2",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "turbo": "^2.9.6"
  }
}
