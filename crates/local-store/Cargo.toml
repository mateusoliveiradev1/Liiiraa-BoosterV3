[package]
name = "local-store"
version = "0.1.0"
description = "SQLite local persistence for optimizer snapshots, audit history, sync queue, and benchmarks."
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
rusqlite = { version = "0.32.1", features = ["bundled"] }

[lints]
workspace = true
