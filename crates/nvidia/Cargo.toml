[package]
name = "nvidia"
version = "0.1.0"
description = "NVIDIA profile backup, planning, apply, and verification for Liiiraa Boost."
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
gpu = { path = "../gpu" }
pubg = { path = "../pubg" }

[lints]
workspace = true
