[package]
name = "optimizer-core"
version = "0.1.0"
description = "Pure tweak planning, policy, validation, and risk modeling for Liiiraa Boost."
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
cpu = { path = "../cpu" }
gpu = { path = "../gpu" }
intel-gpu = { path = "../intel-gpu" }

[lints]
workspace = true
